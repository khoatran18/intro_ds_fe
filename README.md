# intro_ds_fe

## Cấu hình API backend

Chỉnh URL backend trong `config.js`:

```js
window.API_BASE_URL = 'http://localhost:8000';
```

Ví dụ nếu backend chạy ở `http://localhost:5000`, đổi thành:

```js
window.API_BASE_URL = 'http://localhost:5000';
```
